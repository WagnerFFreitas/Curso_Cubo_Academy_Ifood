// A lista também é conhecida como Array.

const lista = [1, 2, "Olá", 4]; // Foi criada a variável LISTA que tem 1, 2, Olá e 4.
console.log (lista)

for (const elemento of lista){ // Foi criada uma variável elemento para ser utilizada dentro da condição FOR para que seja informado quais tipos de elemento tem dentro da variável lista utilizada o TYPEOF.
  console.log (typeof elemento);
}
