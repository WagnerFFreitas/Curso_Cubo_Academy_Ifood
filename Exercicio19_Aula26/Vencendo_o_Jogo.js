// Logica do código

// placar = [3, 2]

// Se a quantidade de gols do Time A for maior que a do Time B
// -> TIME A

//Se não, se a quantidade de gols do Time B é maior que a do TimeA
// -> TIME B

// Caso contrário -> EMPATE

let placar [5, 3];
let quantidadegolstimeA =
if (quantidadegolstimeA > quantidadegolstimeB) {
  console.log("TIME A")
} else if (quantidadegolstimeB > quantidadegolstimeA) {
  console.log ("TIME B")
} else {
console.log ("EMPATE");